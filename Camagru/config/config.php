<?php

  define('DEBUG', true);

  define('DB_NAME', 'camagru'); // database name
  define('DB_USER', 'root'); // database user
  define('DB_PASSWORD', '20121994'); // database password
  define('DB_HOST', 'localhost'); // database host *** use IP address to avoid DNS lookup

  define('DEFAULT_CONTROLLER', 'Home'); //d efault controller if there isn't one defined in url
  define('DEFAULT_LAYOUT', 'default'); // if no layout is set in controller usae this layout.

  define('PROOT', '/ruah/'); // set this to '/' for a live server.

  define('SITE_TITLE', 'Camagru'); // This will be used if no site title is set.
